---
const { locationData } = Astro.props;
const stateName = locationData.name;
const phoneNumber = "0472/80.80.80"; // From brief
const telLink = `tel:+32${phoneNumber.replace(/[/.]/g, '').substring(1)}`;
---
<section class="py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="bg-gradient-to-br from-blue-900 to-blue-800 rounded-3xl overflow-hidden"> 
            <div class="px-8 py-12 md:p-16 text-center">
                 {/* EXACT Heading from Brief */}
                <h2 class="text-3xl md:text-4xl font-bold text-white mb-6 font-montserrat">
                    Contactez nos experts en détection de fuites {stateName}
                </h2>
                 {/* EXACT Paragraphs from Brief */}
                <p class="text-blue-100 text-lg mb-4 max-w-3xl mx-auto">
                     Ne laissez pas une fuite détériorer votre habitat ou gonfler vos factures. Nos spécialistes en détection de fuites interviennent rapidement dans toute la région {stateName} pour localiser avec précision l'origine du problème.
                </p>
                 {/* EXACT Contact Details from Brief */}
                <p class="text-white text-lg font-medium mb-2">
                    Téléphone: <a href={telLink} class="hover:underline">{phoneNumber}</a> (disponible 24h/24)
                </p>
                <p class="text-white text-lg font-medium mb-8">
                    Intervention d'urgence: Service disponible 7j/7
                </p>
                 {/* Button styled like example */}
                <a
                    href="/contact/" 
                    class="inline-block bg-white text-blue-600 px-8 py-4 rounded-xl font-bold hover:bg-blue-50 transition duration-300 shadow-lg font-montserrat"
                >
                    Demandez votre devis gratuit
                </a>
                <p class="text-blue-200 text-sm mt-4">
                    Bénéficiez de l'expertise de nos techniciens certifiés en détection de fuites {stateName}.
                </p>
            </div>
        </div>
    </div>
</section>